<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.trade.mapper.IdempotentKeyMapper">

    <resultMap id="BaseResultMap" type="com.campus.trade.model.entity.IdempotentKey">
        <id column="id" property="id"/>
        <result column="idempotent_key" property="idempotentKey"/>
        <result column="biz_type" property="bizType"/>
        <result column="biz_id" property="bizId"/>
        <result column="created_at" property="createdAt"/>
        <result column="expire_at" property="expireAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_idempotent_key (idempotent_key, biz_type, biz_id, expire_at)
        VALUES (#{idempotentKey}, #{bizType}, #{bizId}, #{expireAt})
    </insert>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT * FROM t_idempotent_key WHERE idempotent_key = #{idempotentKey} AND expire_at > NOW()
    </select>

    <delete id="deleteExpired">
        DELETE FROM t_idempotent_key WHERE expire_at &lt; NOW()
    </delete>
</mapper>
