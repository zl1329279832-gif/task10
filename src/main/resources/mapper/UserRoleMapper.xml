<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.trade.mapper.UserRoleMapper">

    <resultMap id="BaseResultMap" type="com.campus.trade.model.entity.UserRole">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role" property="role"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM t_user_role WHERE user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user_role (user_id, role) VALUES (#{userId}, #{role})
    </insert>

    <delete id="deleteByUserIdAndRole">
        DELETE FROM t_user_role WHERE user_id = #{userId} AND role = #{role}
    </delete>

    <select id="countByUserIdAndRole" resultType="int">
        SELECT COUNT(*) FROM t_user_role WHERE user_id = #{userId} AND role = #{role}
    </select>
</mapper>
